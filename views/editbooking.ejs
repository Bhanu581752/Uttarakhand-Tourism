<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Booking</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-10">
  <h1 class="text-3xl font-bold text-center text-blue-600 mb-8">✏️ Edit Booking</h1>

  <div class="max-w-md mx-auto bg-white p-6 rounded-lg shadow-md">
    <form action="/edit/<%= booking._id %>" method="post" class="space-y-4">
      <div>
        <label class="block mb-1 text-gray-700">Destination</label>
        <input type="text" value="<%= booking.destination %>" readonly
               class="w-full px-3 py-2 border rounded-lg bg-gray-100">
      </div>
      <div>
        <label class="block mb-1 text-gray-700">Travel Date</label>
        <input type="date" name="date" value="<%= booking.date.toISOString().split('T')[0] %>"
               class="w-full px-3 py-2 border rounded-lg">
      </div>
      <div>
        <label class="block mb-1 text-gray-700">Number of Persons</label>
        <input type="number" name="persons" value="<%= booking.persons %>" min="1"
               class="w-full px-3 py-2 border rounded-lg">
      </div>
      <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700">💾 Save Changes</button>
    </form>
  </div>
</body>
</html>
